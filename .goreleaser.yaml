version: 2

project_name: vigilo

before:
  hooks:
    - go mod tidy

builds:
  - id: vigilo
    main: ./cmd/vigilo
    binary: vigilo
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - config.example.yaml
      - config.personal.example.yaml
      - README.md
      - SECURITY.md
      - CHANGELOG.md
      - deploy/vigilo.service
      - deploy/install.sh
      - deploy/user/install.sh
      - deploy/user/run-vigilo.sh
      - deploy/user/vigilo.service
      - deploy/macos/install-launch-agent.py

checksum:
  name_template: checksums.txt
  algorithm: sha256

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^chore:"
      - "^docs:"
      - "^ci:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: agent-rails
    name: vigilo
  # The release workflow publishes only after the archive installation smoke test.
  draft: true
  prerelease: auto
  name_template: "v{{ .Version }}"
  header: |
    ## Vigilo {{ .Version }}

    Personal-system observation and alerting: filesystem
    create/write events, sampled process and network activity, and optional
    supply-chain checks. See CHANGELOG.md for v{{ .Version }} changes and current limits.

    ### Quick start

    ```bash
    tar xzf vigilo_{{ .Version }}_linux_amd64.tar.gz
    bash deploy/user/install.sh
    ```

    Choose the archive matching your operating system and CPU. The installer
    installs a per-user service on Linux or prepares a LaunchAgent on macOS.
    Existing config is preserved. Edit watch paths and alert delivery before
    enabling the service. HTTP MCP requires a bearer token by default.

    See [README](https://github.com/agent-rails/vigilo#readme) and
    [SECURITY.md](https://github.com/agent-rails/vigilo/blob/main/SECURITY.md)
    for full setup instructions.
