{
  "name": "vigilo-agent",
  "version": "0.1.0",
  "description": "LLM analyst agent — connects to vigilo daemon via MCP, detects attack patterns, alerts",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "NODE_ENV=production node --import tsx --test src/agents/analyst.test.ts src/slack/alerter.test.ts src/index.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.test.json"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@slack/web-api": "^7.19.0",
    "node-cron": "^3.0.0",
    "dotenv": "^16.0.0",
    "pino": "^9.0.0",
    "pino-pretty": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/node-cron": "^3.0.0",
    "tsx": "^4.23.12",
    "typescript": "^5.4.0"
  }
}
